# PROJECT GUIDELINES

## 1. 프로젝트 작업 대상

실제 개발 작업은 `ryujmin97/v1` 저장소의 `ryu-v1` 브랜치를 기준으로 한다.

원본 참고 저장소는 `ryujmin97/openpilot`의 `carrot-ryu-v1` 브랜치다.

원본 저장소는 별도의 명시적인 요청이 없는 한 수정하거나 push하지 않는다.

## 2. 작업 원칙

- 작업 시작 전에 Git 상태와 최신 프로젝트 문서를 확인한다.
- 관련 코드를 충분히 확인한 후 최소한으로 수정한다.
- 기존 사용자 변경사항을 임의로 삭제하거나 덮어쓰지 않는다.
- 테스트하지 않은 내용을 테스트했다고 말하지 않는다.
- 민감정보를 코드나 커밋에 포함하지 않는다.

## 3. 차량 제어 및 안전

- 차량 제어 및 safety 관련 변경은 특히 신중하게 다룬다.
- 실차에서 검증되지 않은 변경을 안전하다고 표현하지 않는다.
- 가능하면 시뮬레이션, 정적 분석, 단위 테스트 및 로그 검토를 우선한다.
- Radar detection 또는 lead-selection 변경 시 AGENTS.md의 관련 규칙을 확인한다.

## 4. 변경 후 검증

- git diff --check
- git status
- 관련 테스트 및 정적 분석을 가능한 범위에서 실행한다.

## 5. Git 작업

기본 흐름: ryu-v1 확인 → 최신 상태 확인 → 코드 분석 → 수정 → 테스트 → diff 확인 → commit → push

### 개발 및 원본 반영 워크플로

- 신규 개발 및 수정 작업은 항상 `ryujmin97/v1`의 `ryu-v1` 브랜치에서 우선 진행한다.
- 관련 변경을 `ryu-v1`에 누적하면서 코드 분석, 테스트, 정적 분석 및 실기기/로그 검증을 가능한 범위에서 수행한다.
- 작업이 어느 정도 누적되고 관련 기능이 충분히 정리·검증된 시점에 변경사항을 묶어 `ryujmin97/openpilot`의 `carrot-ryu-v1` 브랜치에 반영한다.
- `carrot-ryu-v1`은 누적 변경사항을 원본 계열에 반영하는 단계에서만 수정하며, 일상적인 개발·실험은 `ryu-v1`에서 수행한다.
- `carrot-ryu-v1`에 반영할 때는 변경 범위와 커밋을 확인하고, 반영 결과를 `docs/WORKLOG.md`에 기록한다.

명시적인 승인 없이 force push, git reset --hard, 기존 커밋 재작성, 대규모 삭제, 브랜치 삭제, 원본 저장소 push를 하지 않는다.

## 6. 작업 기록

중요한 작업은 docs/WORKLOG.md에 날짜, 목적, 변경 파일, 주요 변경, 테스트 결과, 문제, 미해결 사항, 다음 작업을 기록한다.

## 7. 저장소 구분

실제 작업: ryujmin97/v1 → ryu-v1

원본 참고: ryujmin97/openpilot → carrot-ryu-v1
